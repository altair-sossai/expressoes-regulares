# Descrição:
Dado um script SQL Server, remover todos os [dbo], [ e ].

# Entrada
CREATE TABLE [dbo].[TB_PROPOSTA_CONTRATO] (
    [ID_PROPOSTA] UNIQUEIDENTIFIER NOT NULL,
    [DH_ARMAZENAMENTO] DATETIME2 NOT NULL CONSTRAINT [DF_TB_PROPOSTA_CONTRATO_DH_ARMAZENAMENTO] DEFAULT GETDATE(),
    [CONTRATO] VARBINARY(MAX) NOT NULL,
    [EXTENSAO_CONTRATO] NVARCHAR(300) NOT NULL,
    CONSTRAINT [PK_TB_PROPOSTA_CONTRATO] PRIMARY KEY ([ID_PROPOSTA])
);

ALTER TABLE
    [dbo].[TB_PROPOSTA_CONTRATO]
ADD
    CONSTRAINT [FK_CONTRATO_PROPOSTA] FOREIGN KEY ([ID_PROPOSTA]) REFERENCES [dbo].[TB_PROPOSTA] ([ID]);

CREATE TABLE [dbo].[TB_PROPOSTA_LOG] (
    [ID] UNIQUEIDENTIFIER NOT NULL,
    [ID_PROPOSTA] UNIQUEIDENTIFIER NOT NULL,
    [DH_PROCESSAMENTO] DATETIME2 NOT NULL,
    [DH_EVENTO] DATETIME2 NOT NULL,
    [ST_OPERACAO] INT NOT NULL,
    [CD_OPERADOR] NVARCHAR(200) NOT NULL,
    [CD_LOJA] NVARCHAR(200) NOT NULL,
    [TP_ACAO] INT NOT NULL,
    [DL_PAYLOAD] NVARCHAR(MAX),
    [FG_SUCESSO] BIT NOT NULL,
    CONSTRAINT [PK_TB_PROPOSTA_LOG] PRIMARY KEY ([ID])
);

ALTER TABLE
    [dbo].[TB_PROPOSTA_LOG]
ADD
    CONSTRAINT [FK_PROPOSTA_LOG] FOREIGN KEY ([ID_PROPOSTA]) REFERENCES [dbo].[TB_PROPOSTA] ([ID]);

# Solução
\[dbo\]\.|\[(\w+)\]

# Replace
$1